JSIGHT 0.3

URL /api/http
  GET
  POST

URL /api/rpc

  Method createCat // Create a cat.
    Description
        The method creates a better cat.
    Params
      {
        "cat": @cat
      }
    Result
      {
        "id": 1 // Cat’s id.
      }

  Method getCat // Get a cat by its id.
    Params
      {
        "id": 1 // Cat’s id.
      }
    Result
      @cat

  Protocol json-rpc-2.0

TYPE @cat
{
  "id": 1,
  "name": "Tom"
}
